name: rby
description: An opinionated widgets & utilities library
version: 0.0.1
homepage: https://github.com/robertodoering/rby.dart
publish: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
