name: example
description: rby_widgets example

publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.2
  flutter_slidable: ^2.0.0
  rby:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true